from fastapi import APIRouter

api_router = APIRouter()

# You can include future endpoints here, example:
# from app.api.v1 import users
# api_router.include_router(users.router, prefix="/users", tags=["users"])

